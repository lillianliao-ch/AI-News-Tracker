## DeepAgents-style Web UI for LinkedIn Batch Parsing

Reference: [sagar-n/deepagents](https://github.com/sagar-n/deepagents)

This is a lightweight Gradio UI that batches LinkedIn profile URLs, calls the local parsing service endpoints, and exports an aggregated Excel for download.

Run:
```bash
pip install -r requirements.txt
python app.py
```

