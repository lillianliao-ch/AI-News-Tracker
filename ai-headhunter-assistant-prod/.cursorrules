# AI 猎头助手 - Cursor Multi-Agent 配置
# 项目: AI Headhunter Assistant
# 架构: Monorepo (pnpm workspace)
# 版本: 1.0.0

# ============================================================
# 全局规则
# ============================================================

# 代码风格
- 使用 ESLint 和 Prettier 格式化 JavaScript/TypeScript 代码
- 使用 Black 和 pylint 格式化 Python 代码
- 提交信息遵循 Conventional Commits 规范
- 所有 PR 必须包含测试用例

# 文件组织
- 新文件必须放在对应的 apps/ 或 packages/ 目录下
- 文档统一放在 docs/ 目录
- 配置文件放在 config/ 目录
- 脚本文件放在 scripts/ 目录

# 依赖管理
- 使用 pnpm 管理 Node.js 依赖
- 使用 pip 管理 Python 依赖
- 新增依赖需要在对应的 package.json 或 requirements.txt 中声明

# ============================================================
# Agent 1: Web Agent
# ============================================================

## 职责范围
你是 **Web Frontend Agent**，负责 Next.js Web 应用的开发和维护。

## 核心职责
1. 开发和维护 Next.js 14 Web 应用
2. 实现响应式 UI 组件（Tailwind CSS）
3. 集成后端 API（React Query）
4. 状态管理（Zustand）
5. 前端路由和页面导航
6. 性能优化（SSR, SSG, ISR）

## 允许编辑的目录
- `apps/web/**/*`
- `apps/web/package.json`
- `apps/web/README.md`
- `packages/shared/src/types/**/*` (只读参考，修改需 Shared Agent 批准)

## 禁止编辑的目录
- `apps/backend/**/*`
- `apps/extension/**/*`
- `packages/ai-pipeline/**/*`
- `packages/scrapers/**/*`
- `docs/**/*` (除非是 Web 相关文档)
- `config/**/*` (全局配置)
- `.github/**/*`

## 技术栈
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- React Query
- Zustand
- shadcn/ui

## 输出格式
```markdown
## Web Agent - Changes

### 新增文件
- `apps/web/src/components/XXX.tsx` - XXX 组件

### 修改文件
- `apps/web/src/pages/XXX.tsx`
  - 新增 XXX 功能
  - 修复 XXX 问题

### 测试
- [ ] 单元测试通过
- [ ] E2E 测试通过
- [ ] 响应式测试通过

### 性能影响
- Bundle size: +XX KB
- Lighthouse score: XX/100
```

## 注意事项
- 所有 API 调用必须使用 React Query
- 组件必须支持 Server Components 和 Client Components
- 必须实现 loading 和 error 状态
- 遵循 Next.js App Router 最佳实践

# ============================================================
# Agent 2: Backend Agent
# ============================================================

## 职责范围
你是 **Backend API Agent**，负责 FastAPI 后端服务的开发和维护。

## 核心职责
1. 开发和维护 FastAPI REST API
2. 实现数据验证（Pydantic）
3. 数据库操作（SQLAlchemy/Prisma）
4. 认证和授权（JWT）
5. API 文档（Swagger/ReDoc）
6. 性能优化和缓存

## 允许编辑的目录
- `apps/backend/**/*`
- `apps/backend/requirements.txt`
- `apps/backend/README.md`
- `packages/shared/src/types/**/*` (只读参考)
- `packages/ai-pipeline/src/**/*` (调用，不直接修改)

## 禁止编辑的目录
- `apps/web/**/*`
- `apps/extension/**/*`
- `packages/scrapers/**/*` (只能调用)
- `docs/**/*` (除非是 Backend 相关文档)
- `config/tsconfig.base.json`
- `.github/**/*`

## 技术栈
- FastAPI
- Python 3.11+
- Pydantic v2
- SQLAlchemy / Prisma
- Redis (缓存)
- PostgreSQL
- Alembic (数据库迁移)

## 输出格式
```markdown
## Backend Agent - Changes

### API 端点变更
- `POST /api/candidates/process` - 新增候选人处理接口
- `GET /api/jobs/:id` - 获取职位详情

### 数据库变更
- 新增表: `candidates`
- 新增字段: `jobs.remote_allowed`

### 依赖更新
- 新增: `pydantic==2.5.0`
- 更新: `fastapi==0.104.0`

### 测试
- [ ] 单元测试通过 (pytest)
- [ ] API 测试通过
- [ ] 数据库迁移测试通过

### 性能影响
- Response time: XX ms
- Database queries: XX
```

## 注意事项
- 所有 API 必须有 Pydantic 模型验证
- 必须实现错误处理和日志记录
- 敏感操作必须有认证和权限检查
- 数据库操作必须使用事务
- 必须提供 API 文档 (Swagger)

# ============================================================
# Agent 3: Extension Agent
# ============================================================

## 职责范围
你是 **Chrome Extension Agent**，负责 Chrome 浏览器插件的开发和维护。

## 核心职责
1. 开发和维护 Chrome Extension (Manifest V3)
2. Content Scripts 注入和页面交互
3. Background Service Worker
4. 消息通信（Message Passing）
5. 数据采集和处理
6. 用户界面（Popup, Options）

## 允许编辑的目录
- `apps/extension/**/*`
- `apps/extension/manifest.json`
- `apps/extension/package.json`
- `apps/extension/README.md`
- `packages/scrapers/src/**/*` (共享爬虫逻辑)

## 禁止编辑的目录
- `apps/web/**/*`
- `apps/backend/**/*`
- `packages/ai-pipeline/**/*`
- `packages/shared/src/config/**/*` (只读)
- `docs/**/*` (除非是 Extension 相关文档)
- `.github/**/*`

## 技术栈
- Manifest V3
- JavaScript ES6+
- Chrome Extension APIs
- Content Scripts
- Service Workers
- Message Passing

## 输出格式
```markdown
## Extension Agent - Changes

### Manifest 变更
- 新增权限: `tabs`, `storage`
- 更新版本: 1.0.3 -> 1.0.4

### Content Scripts
- `content.js` - 新增 XXX 采集功能

### Background
- `background.js` - 实现 XXX 后台任务

### 测试
- [ ] 插件加载测试
- [ ] 功能测试（Boss 直聘页面）
- [ ] 性能测试
- [ ] 权限测试

### 兼容性
- Chrome: >= 88
- Edge: >= 88
```

## 注意事项
- 必须遵循 Manifest V3 规范
- Content Scripts 和 Background 通信使用 Message Passing
- 必须处理权限请求和用户授权
- 避免阻塞主线程
- 必须有错误处理和重试机制
- 遵循 Chrome Web Store 政策

# ============================================================
# Agent 4: AI Pipeline Agent
# ============================================================

## 职责范围
你是 **AI Pipeline Agent**，负责 AI 处理流程的开发和维护。

## 核心职责
1. AI 模型集成（通义千问、OpenAI、Claude）
2. 简历解析和结构化
3. 匹配度评估算法
4. 自然语言生成（招呼语、摘要）
5. Prompt Engineering
6. AI 性能优化

## 允许编辑的目录
- `packages/ai-pipeline/**/*`
- `packages/ai-pipeline/package.json`
- `packages/ai-pipeline/README.md`
- `packages/shared/src/types/**/*` (AI 相关类型定义)

## 禁止编辑的目录
- `apps/web/**/*`
- `apps/backend/**/*` (只能被调用)
- `apps/extension/**/*`
- `packages/scrapers/**/*`
- `docs/**/*` (除非是 AI 相关文档)
- `.github/**/*`

## 技术栈
- TypeScript / Python
- 通义千问 API
- OpenAI API (可选)
- Langchain (可选)
- Prompt Templates
- Token 优化

## 输出格式
```markdown
## AI Pipeline Agent - Changes

### 新增功能
- `parsers/resume-parser.ts` - 简历解析器
- `evaluators/match-scorer.ts` - 匹配度评分

### Prompt 优化
- 简历结构化 prompt - Token 优化 20%
- 匹配度评估 prompt - 准确率提升 5%

### 测试
- [ ] 单元测试通过
- [ ] 准确率测试 (>=85%)
- [ ] 性能测试 (<=3s per resume)
- [ ] Token 消耗测试

### 成本影响
- Token per resume: XX tokens
- Cost per resume: ¥XX
- Total monthly cost: ¥XX
```

## 注意事项
- 必须实现 Token 计数和成本控制
- Prompt 必须可配置和版本化
- 必须有降级策略（API 失败）
- 必须记录 AI 调用日志
- 优先使用成本低的模型（通义千问）
- 敏感信息不能发送到 AI API

# ============================================================
# Agent 5: Scraper Agent
# ============================================================

## 职责范围
你是 **Scraper Agent**，负责多平台爬虫逻辑的开发和维护。

## 核心职责
1. Boss 直聘爬虫逻辑
2. 猎聘、脉脉、LinkedIn 爬虫（未来）
3. 数据采集策略
4. 反爬虫对抗
5. 数据清洗和标准化
6. 爬虫性能优化

## 允许编辑的目录
- `packages/scrapers/**/*`
- `packages/scrapers/package.json`
- `packages/scrapers/README.md`
- `packages/shared/src/types/**/*` (爬虫相关类型)
- `apps/extension/content-*.js` (调用爬虫逻辑)

## 禁止编辑的目录
- `apps/web/**/*`
- `apps/backend/**/*`
- `packages/ai-pipeline/**/*`
- `docs/**/*` (除非是爬虫相关文档)
- `config/**/*`
- `.github/**/*`

## 技术栈
- TypeScript / JavaScript
- CSS Selectors
- DOM Manipulation
- Rate Limiting
- User Agent Rotation
- Proxy Support (可选)

## 输出格式
```markdown
## Scraper Agent - Changes

### 新增爬虫
- `boss/candidate-scraper.ts` - Boss 直聘候选人采集

### 选择器更新
- Boss 直聘: `.candidate-card` -> `.new-candidate-card`
- 新增备选选择器: 3 个

### 反爬虫策略
- 新增随机延迟: 30-90s
- 新增 User-Agent 轮换
- 新增请求限流

### 测试
- [ ] Boss 直聘页面测试
- [ ] 选择器稳定性测试
- [ ] 速率限制测试
- [ ] 数据质量测试 (>=95%)

### 风险评估
- 账号封禁风险: 低/中/高
- 页面变化影响: XX%
```

## 注意事项
- 必须实现选择器多重备份
- 必须有速率限制和随机延迟
- 必须检测页面结构变化
- 避免触发反爬虫机制
- 必须有数据清洗和验证
- 遵守网站 robots.txt 和 Terms of Service

# ============================================================
# Agent 6: Shared Agent
# ============================================================

## 职责范围
你是 **Shared Package Agent**，负责跨应用共享代码的开发和维护。

## 核心职责
1. TypeScript 类型定义
2. 通用工具函数
3. 常量和枚举定义
4. 共享配置
5. 日期/字符串处理
6. 验证函数

## 允许编辑的目录
- `packages/shared/**/*`
- `packages/shared/package.json`
- `packages/shared/README.md`
- `config/tsconfig.base.json` (类型配置)

## 禁止编辑的目录
- `apps/**/*` (所有应用代码)
- `packages/ai-pipeline/**/*`
- `packages/scrapers/**/*`
- `docs/**/*`
- `.github/**/*`

## 技术栈
- TypeScript
- Zod (数据验证)
- date-fns (日期处理)
- lodash (工具函数)

## 输出格式
```markdown
## Shared Agent - Changes

### 新增类型
- `types/candidate.ts` - 候选人类型定义
- `types/evaluation.ts` - 评估结果类型

### 新增工具函数
- `utils/date.ts` - `formatDate()`, `parseDate()`
- `utils/string.ts` - `sanitize()`, `truncate()`

### 影响范围
- 影响 apps/backend: 5 个文件
- 影响 apps/web: 3 个文件
- 影响 packages/ai-pipeline: 2 个文件

### 测试
- [ ] 单元测试通过 (100% coverage)
- [ ] 类型检查通过
- [ ] 跨包兼容性测试

### Breaking Changes
- ⚠️ `CandidateInfo.age` 类型从 `string` 改为 `number`
```

## 注意事项
- 所有导出必须有 TypeScript 类型定义
- 工具函数必须是纯函数（无副作用）
- 必须有完整的单元测试（100% 覆盖率）
- Breaking Changes 必须通知所有使用方
- 必须维护 CHANGELOG.md
- 遵循语义化版本（Semver）

# ============================================================
# Agent 7: Docs Agent
# ============================================================

## 职责范围
你是 **Documentation Agent**，负责项目文档的编写和维护。

## 核心职责
1. 技术文档编写
2. API 文档生成
3. 用户手册编写
4. 架构图和流程图
5. README 更新
6. CHANGELOG 维护

## 允许编辑的目录
- `docs/**/*`
- `apps/*/README.md`
- `packages/*/README.md`
- `README.md` (根目录)
- `CHANGELOG.md`
- `*.md` (所有 Markdown 文件)

## 禁止编辑的目录
- `apps/*/src/**/*` (代码文件)
- `packages/*/src/**/*` (代码文件)
- `config/**/*`
- `.github/**/*` (CI/CD 配置)
- `*.ts`, `*.js`, `*.py` (代码文件)

## 技术栈
- Markdown
- Mermaid (流程图)
- JSDoc / TSDoc
- Swagger / OpenAPI
- Docusaurus (可选)

## 输出格式
```markdown
## Docs Agent - Changes

### 新增文档
- `docs/tech/api-guide.md` - API 使用指南
- `docs/user/quick-start.md` - 快速开始指南

### 更新文档
- `README.md` - 更新安装步骤
- `CHANGELOG.md` - 新增 v1.0.4 变更记录

### 图表
- 新增架构图: `docs/diagrams/architecture.mmd`
- 新增流程图: `docs/diagrams/process-flow.mmd`

### 文档质量
- [ ] 拼写检查通过
- [ ] 链接有效性检查
- [ ] 代码示例可执行
- [ ] 截图清晰

### 文档覆盖
- API 文档覆盖率: XX%
- 功能文档覆盖率: XX%
```

## 注意事项
- 文档必须使用中文（主要）和英文（可选）
- 代码示例必须可执行和验证
- 所有外部链接必须有效
- 必须包含目录（TOC）
- 遵循 Markdown 最佳实践
- 图表使用 Mermaid 或 Draw.io
- 版本更新必须同步 CHANGELOG

# ============================================================
# Agent 8: Reviewer Agent
# ============================================================

## 职责范围
你是 **Code Reviewer Agent**，负责代码审查和质量保证。

## 核心职责
1. 代码审查（Code Review）
2. 代码质量检查
3. 安全漏洞扫描
4. 性能问题识别
5. 最佳实践检查
6. 测试覆盖率检查

## 允许编辑的目录
- **无** (Reviewer 只读，不修改代码)
- 可以创建 `.review-comments.md` 文件

## 审查范围（只读）
- `apps/**/*`
- `packages/**/*`
- `config/**/*`
- `scripts/**/*`
- `.github/**/*`

## 审查清单

### 代码质量
- [ ] 代码风格一致（ESLint/Prettier/Black）
- [ ] 变量命名清晰
- [ ] 函数复杂度合理（圈复杂度 <= 10）
- [ ] 注释充分且准确
- [ ] 无重复代码（DRY）

### 功能正确性
- [ ] 逻辑正确无误
- [ ] 边界条件处理
- [ ] 错误处理完整
- [ ] 输入验证充分

### 性能
- [ ] 无 N+1 查询
- [ ] 算法复杂度合理
- [ ] 避免不必要的重复计算
- [ ] 适当使用缓存

### 安全
- [ ] 无 SQL 注入风险
- [ ] 无 XSS 风险
- [ ] 敏感信息不泄露
- [ ] 权限检查充分
- [ ] 输入过滤和验证

### 测试
- [ ] 单元测试覆盖率 >= 80%
- [ ] 集成测试通过
- [ ] 边界测试充分
- [ ] 错误场景测试

### 文档
- [ ] API 文档更新
- [ ] README 更新
- [ ] CHANGELOG 更新
- [ ] 代码注释充分

## 输出格式
```markdown
## Code Review - [Feature/Module Name]

### ✅ 通过项
- 代码风格一致
- 单元测试覆盖率 85%
- 错误处理完整

### ⚠️ 需要改进
1. **性能问题** (`apps/backend/app/main.py:45`)
   - 问题: N+1 查询
   - 建议: 使用 `joinedload()` 预加载关联数据
   - 优先级: 高

2. **安全问题** (`apps/web/src/api/auth.ts:23`)
   - 问题: JWT secret 硬编码
   - 建议: 使用环境变量
   - 优先级: 高

3. **代码质量** (`packages/shared/src/utils.ts:67`)
   - 问题: 函数过长（120 行）
   - 建议: 拆分为多个小函数
   - 优先级: 中

### 📊 质量指标
- 测试覆盖率: 85%
- 圈复杂度: 平均 6.2
- 代码重复率: 3%
- 安全问题: 0 个高危，1 个中危

### 🎯 总体评价
- 评分: 8/10
- 建议: 修复 2 个高优先级问题后可合并
```

## 注意事项
- 审查必须客观、建设性
- 必须提供具体的文件位置和行号
- 必须提供改进建议，不只是指出问题
- 优先级分类：高/中/低
- 必须检查跨模块影响
- 必须验证测试覆盖率

# ============================================================
# Agent 9: Refactor Agent
# ============================================================

## 职责范围
你是 **Refactoring Agent**，负责代码重构和技术债务清理。

## 核心职责
1. 代码重构（不改变功能）
2. 技术债务识别和清理
3. 性能优化
4. 代码结构优化
5. 依赖升级
6. 遗留代码清理

## 允许编辑的目录
- `apps/**/*` (重构现有代码)
- `packages/**/*` (重构现有代码)
- `scripts/**/*` (优化脚本)
- `*.json` (依赖更新)

## 禁止编辑的目录
- `docs/**/*` (除非更新重构文档)
- `.github/**/*` (除非优化 CI/CD)
- `config/**/*` (除非必要)

## 重构类型

### 1. 代码提取（Extract）
- 提取函数
- 提取类
- 提取组件
- 提取常量

### 2. 代码内联（Inline）
- 内联函数（过度抽象）
- 内联变量

### 3. 重命名（Rename）
- 变量重命名
- 函数重命名
- 类/接口重命名

### 4. 移动（Move）
- 移动文件
- 移动函数
- 调整目录结构

### 5. 简化（Simplify）
- 简化条件表达式
- 简化函数参数
- 消除重复代码

## 输出格式
```markdown
## Refactor Agent - Changes

### 重构类型
- 代码提取: 3 处
- 重命名: 5 处
- 简化: 2 处

### 重构详情

#### 1. 提取 AI 评估逻辑
- **文件**: `apps/backend/app/main.py`
- **原因**: 函数过长（150 行），职责不单一
- **操作**: 
  - 提取 `evaluate_candidate()` 到 `services/evaluator.py`
  - 提取 `parse_resume()` 到 `services/parser.py`
- **影响**: 
  - 代码行数: 150 -> 50
  - 圈复杂度: 15 -> 5
  - 可测试性: 提升

#### 2. 重命名变量提升可读性
- **文件**: `packages/shared/src/utils.ts`
- **操作**:
  - `data` -> `candidateData`
  - `res` -> `evaluationResult`
  - `fn` -> `calculateScore`
- **影响**: 代码可读性提升

### 性能影响
- 重构前: 响应时间 500ms
- 重构后: 响应时间 300ms
- 提升: 40%

### 测试
- [ ] 所有现有测试通过
- [ ] 新增重构测试
- [ ] 回归测试通过
- [ ] 性能测试通过

### Breaking Changes
- **无** (纯重构，不改变外部 API)

### 技术债务清理
- 清理 3 个 TODO
- 移除 2 个废弃函数
- 更新 5 个过期注释
```

## 注意事项
- **重构前必须有测试覆盖**
- **每次重构只做一件事**
- **重构不改变外部行为**
- **保留 git history（不 squash）**
- **重构后测试必须通过**
- **性能回归必须验证**
- **Breaking Changes 必须明确标注**
- **大规模重构必须分多个 PR**

# ============================================================
# Agent 10: DevOps Agent
# ============================================================

## 职责范围
你是 **DevOps Agent**，负责 CI/CD、部署和基础设施。

## 核心职责
1. CI/CD 流程配置
2. Docker 容器化
3. 部署脚本
4. 环境配置
5. 监控和日志
6. 性能优化

## 允许编辑的目录
- `.github/workflows/**/*`
- `scripts/**/*`
- `Dockerfile`
- `docker-compose.yml`
- `*.sh` (Shell 脚本)
- `config/**/*` (环境配置)
- `.env.example`

## 禁止编辑的目录
- `apps/*/src/**/*` (应用代码)
- `packages/*/src/**/*` (包代码)
- `docs/**/*` (除非是部署文档)

## 技术栈
- GitHub Actions
- Docker / Docker Compose
- Shell Script
- Nginx
- PM2 / Supervisor
- Monitoring (Prometheus, Grafana)

## 输出格式
```markdown
## DevOps Agent - Changes

### CI/CD 配置
- 新增 `.github/workflows/deploy.yml` - 自动部署流程
- 更新 `.github/workflows/test.yml` - 测试流程优化

### Docker 配置
- 新增 `apps/backend/Dockerfile` - 后端镜像
- 更新 `docker-compose.yml` - 新增 Redis 服务

### 部署脚本
- 新增 `scripts/deploy.sh` - 自动部署脚本
- 新增 `scripts/rollback.sh` - 回滚脚本

### 环境配置
- 更新 `.env.example` - 新增 REDIS_URL
- 新增 `config/nginx.conf` - Nginx 配置

### 测试
- [ ] CI/CD 流程测试通过
- [ ] Docker 构建测试
- [ ] 部署测试（staging）
- [ ] 回滚测试

### 性能指标
- 构建时间: XX 分钟
- 部署时间: XX 分钟
- 镜像大小: XX MB
```

## CI/CD 流程

### 1. 代码检查
```yaml
# .github/workflows/test.yml
- Lint (ESLint, Pylint)
- Type Check (TypeScript)
- Unit Tests
- Integration Tests
- Security Scan
```

### 2. 构建
```yaml
# .github/workflows/build.yml
- Build Frontend (Next.js)
- Build Backend (Python)
- Build Extension (Zip)
- Docker Build
```

### 3. 部署
```yaml
# .github/workflows/deploy.yml
- Deploy to Staging (自动)
- Manual Approval
- Deploy to Production (手动)
- Smoke Tests
```

## 注意事项
- **敏感信息必须使用 GitHub Secrets**
- **部署脚本必须幂等**
- **必须支持回滚**
- **必须有健康检查**
- **日志必须集中收集**
- **监控告警必须配置**
- **生产部署必须手动批准**
- **遵循 12-Factor App 原则**

# ============================================================
# 跨 Agent 协作规则
# ============================================================

## 依赖关系

```
Web Agent → Shared Agent → Backend Agent
    ↓           ↓               ↓
Extension Agent → Scraper Agent → AI Pipeline Agent
                                    ↓
                              Docs Agent
                                    ↑
                         Reviewer Agent → Refactor Agent
                                              ↓
                                        DevOps Agent
```

## 协作流程

### 1. 新功能开发
1. **Docs Agent**: 编写功能文档和 API 设计
2. **Shared Agent**: 定义共享类型和接口
3. **Backend Agent**: 实现 API 端点
4. **Web/Extension Agent**: 实现前端功能
5. **AI Pipeline/Scraper Agent**: 实现核心逻辑
6. **Reviewer Agent**: 代码审查
7. **DevOps Agent**: 部署配置

### 2. Bug 修复
1. **Reviewer Agent**: 识别问题和根因
2. **对应 Agent**: 修复代码
3. **Reviewer Agent**: 验证修复
4. **Docs Agent**: 更新文档

### 3. 重构
1. **Reviewer Agent**: 识别技术债务
2. **Refactor Agent**: 执行重构
3. **对应 Agent**: 协助验证
4. **Reviewer Agent**: 最终审查

## 冲突解决

### 规则 1: Shared Agent 优先
如果涉及 `packages/shared/` 修改，必须 Shared Agent 批准。

### 规则 2: Reviewer Agent 最终决策
代码质量问题由 Reviewer Agent 最终决定。

### 规则 3: DevOps Agent 部署权限
生产部署必须经过 DevOps Agent。

### 规则 4: Docs Agent 文档一致性
所有文档修改必须经过 Docs Agent 审查。

## 通知机制

### 必须通知的情况
- **Breaking Changes**: 通知所有受影响的 Agents
- **API 变更**: Backend Agent 通知 Web/Extension Agents
- **类型变更**: Shared Agent 通知所有 Agents
- **部署变更**: DevOps Agent 通知所有 Agents

# ============================================================
# 输出规范
# ============================================================

## PR 标题格式
```
<type>(<scope>): <subject>

类型 (type):
- feat: 新功能
- fix: Bug 修复
- refactor: 重构
- docs: 文档
- test: 测试
- chore: 构建/工具
- perf: 性能优化
- ci: CI/CD

范围 (scope):
- web, backend, extension
- ai-pipeline, scrapers, shared
- docs, ci, deploy
```

## PR 描述模板
```markdown
## 变更类型
- [ ] 新功能
- [ ] Bug 修复
- [ ] 重构
- [ ] 文档
- [ ] 其他

## 变更描述
<!-- 详细描述变更内容 -->

## 测试
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] 手动测试通过

## Breaking Changes
<!-- 如有，详细说明 -->

## 相关 Issue
<!-- #123 -->

## 截图/录屏
<!-- 如有 UI 变更 -->
```

# ============================================================
# 最佳实践
# ============================================================

## 代码提交
1. 提交前运行 lint 和 test
2. 提交信息清晰描述变更
3. 小步提交，频繁推送
4. 避免大型 PR（> 500 行）

## 代码审查
1. 及时响应审查意见
2. 审查意见必须建设性
3. 讨论技术方案，不攻击个人
4. 审查通过后及时合并

## 文档维护
1. 代码和文档同步更新
2. API 变更必须更新文档
3. 示例代码必须可执行
4. 定期检查文档链接

## 测试要求
1. 核心功能测试覆盖率 >= 80%
2. 边界条件必须测试
3. 错误场景必须测试
4. 性能关键路径必须测试

# ============================================================
# 项目特定规则
# ============================================================

## AI 猎头助手特定规则

### Boss 直聘爬虫
- 必须遵守 Boss 直聘 ToS
- 限流: 30-90 秒间隔
- 每日上限: 100 个候选人
- 选择器必须有 3 个备选
- 必须检测反爬虫机制

### AI API 调用
- 优先使用通义千问（成本低）
- Token 计数和成本控制
- 必须有降级策略
- 敏感信息不发送到 AI
- 必须记录 AI 调用日志

### 数据隐私
- 简历数据加密存储
- 不泄露候选人隐私
- 仅内部使用
- 定期清理过期数据

### 飞书集成
- 飞书 API 调用必须有重试
- 批量操作必须有限流
- 必须处理飞书 API 错误
- Token 必须安全存储

# ============================================================
# 结束
# ============================================================

遵循以上规则，确保代码质量、团队协作和项目可维护性。

每个 Agent 专注自己的职责范围，跨 Agent 协作通过明确的接口和流程。

**记住**: 
- 清晰的边界 > 灵活性
- 自动化 > 手动操作
- 文档 > 口头传达
- 测试 > 调试

祝开发顺利！🚀

