{
  "name": "@ai-headhunter/backend",
  "version": "1.0.0",
  "description": "AI 猎头助手 - FastAPI 后端服务",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": "pytest tests/",
    "lint": "pylint app/",
    "format": "black app/"
  },
  "keywords": [
    "fastapi",
    "ai",
    "resume",
    "parser"
  ],
  "author": "Lillian Liao",
  "license": "MIT",
  "engines": {
    "python": ">=3.11"
  }
}

